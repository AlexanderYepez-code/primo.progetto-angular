import { HighlightAbsentDirective } from './highlight-absent.directive';

describe('HighlightAbsentDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightAbsentDirective();
    expect(directive).toBeTruthy();
  });
});
